# تقرير Stage06 — Invoice Approval & Debt Ledger Contract

## النسخة الأساسية

- Branch base: `agent/generators-mobile-stage05-monthly-billing`
- Stage05 commit: `d78d44384dc798f3e2f4eaa49d556412d110680f`
- لا تغيير لـRender Service أو Base URL.

## ما تم تنفيذه في السيرفر

- جدول فواتير معتمدة غير قابلة للتعديل أو الحذف.
- سجل ذمم Append-only.
- اعتماد مسودة مراجَعة فقط.
- إنشاء الفاتورة وقيد الذمة داخل معاملة واحدة.
- Idempotency عند إعادة الاعتماد.
- رقم فاتورة فريد وSnapshot كامل.
- عرض الفواتير وملخص المبالغ والمتبقي.
- عرض سجل الذمم وأرصدة القيود.
- Tenant Isolation وAudit Log.
- إبقاء التحصيل والوصولات والصندوق مقفلة.

## ما تم تنفيذه في Flutter

- Invoice API Client وRepository مع Cache.
- Models للفواتير والملخص وسجل الذمم.
- شاشة صاحب المولدة لاختيار الدورة واعتماد المسودات المراجَعة.
- عرض الفواتير المعتمدة والمتبقي وسجل الذمم.
- تأكيد واضح قبل فتح الذمة.
- ربط الشاشة بالواجهة الرئيسية لصاحب المولدة فقط.
- تحديث الإصدار إلى `0.6.0+6`.

## الاختبارات المنفذة

```text
Stage06 runtime harness: passed
Stage06 source contracts: passed
Dart textual checks: passed
```

## ما لم ينفذ عمدًا

- لا تحصيل كامل أو جزئي.
- لا تسديد دين.
- لا وصولات أو طباعة.
- لا تأثير على الصندوق.
- لا إلغاء أو تعديل فاتورة.
- لا تعديل أو حذف سجل الذمة.
- لا GPS أو إشعارات أو واتساب.

## المرحلة التالية المقترحة

`Stage07 — Collection & Receipt Contract`

فتح القبض الكامل أو الجزئي بقيد دائن ووصل Idempotent بعد اعتماد قواعد الصندوق والتسوية.
