abstract final class AppConfig {
  static const String appName = 'النخبة لإدارة المولدات والجباية';
  static const String baseUrl = 'https://modeer-app.onrender.com';
  static const String generatorsBasePath = '/generators';
  static const Duration connectTimeout = Duration(seconds: 20);
  static const Duration receiveTimeout = Duration(seconds: 30);
}
