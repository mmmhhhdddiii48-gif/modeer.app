# Render production values
NODE_ENV=production
HOST=0.0.0.0
# Render provides PORT automatically. Do not hardcode it unless you know why.
DB_FILE=/var/data/nukhba.sqlite
CORS_ORIGIN=*
AUTH_TOKEN_SECRET=replace-with-a-long-random-secret-before-deploy
AUTH_TOKEN_EXPIRES_IN=8h
